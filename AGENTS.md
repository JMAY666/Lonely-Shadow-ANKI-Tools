# 项目 Agent 规则

开始工作前，必须阅读并遵循 [CLAUDE.md](CLAUDE.md) 的全部现有要求，
包括通过 `just` 配方构建、运行、检查和测试，以及其中引用的个人配置。
本文件追加 Git 管理规则，不替代或删减 `CLAUDE.md` 的要求。

## 1. 仓库状态与项目边界

- 开始修改前检查仓库根目录、当前分支、工作区和暂存区状态、已有历史及远程配置。
- 已有仓库沿用现有配置和历史，不重新初始化或覆盖历史；只有确认当前项目不属于
  任何仓库时才初始化。若属于上级仓库，先核实项目边界，边界不明确时向用户确认，
  不擅自建立嵌套仓库。
- 保留现有子模块及其配置，不将子模块内容作为主仓库文件重新纳入。
- 保留用户已有的未提交修改，明确本次工作范围，不擅自回退或夹带无关修改。

## 2. 排除规则与数据保护

- 按实际目录结构维护 `.gitignore`，排除依赖、虚拟环境、缓存、日志、临时文件、
  可重新生成的构建产物、密钥、凭据和个人数据。
- 排除只表示不纳入版本管理，不删除本地文件。不得用清理命令代替停止跟踪。
- 保留运行、开发、测试和复现所需的源码、配置模板及依赖锁定文件。
  本项目的 `build/` 和 `tools/coverage/` 是源码；根目录的 `Cargo.lock`、
  `uv.lock`、`yarn.lock` 以及 `.idea.dist/`、`.vscode.dist/` 应保留。
- 个人 Anki 数据库、牌组导出、媒体、配置和备份默认不入库。现有必要测试样本使用
  精确路径例外保留；新增样本须审核内容并去除个人数据后，才能增加对应例外。
- 配置模板只能包含占位值，不得嵌入真实凭据。修改排除规则后，使用
  `git check-ignore --no-index` 验证应排除和应保留的路径，并检查
  `git ls-files -ci --exclude-standard` 是否发现已跟踪文件仍命中排除规则。

## 3. 审核、检查与本地提交

- 新仓库审核文件清单和内容后创建初始提交；已有仓库只提交实际相关变更。
- 以后每完成一项完整修改并通过相关检查后，创建一次本地提交，提交说明清楚描述
  修改内容。没有实际变更不创建空提交。
- 检查遵循 `CLAUDE.md`，完成前运行 `just check`；按修改范围补充相关检查。
  检查失败时先修复再提交；如因环境缺少工具或依赖无法执行，必须明确记录原因、
  已执行的替代检查及未验证范围，不得宣称检查通过。
- 逐项审核工作区差异、新增文件内容和敏感信息；仅用明确文件路径暂存本次已审核
  内容，不用 `git add .` 或 `git add -A` 兜底。
- 提交前检查 `git diff --cached --name-status`、`git diff --cached`，并确保
  `git diff --cached --check` 通过；提交后复核提交内容、工作区状态和剩余修改。

## 4. 推送授权与完整范围审核

- 用户已明确授权：每次完成提交并审核后，推送到 `https://github.com/JMAY666/ANKI.git` 的 `main`，无需重复确认推送权限。此前仅推送插件分支的规则已被本次授权替代。
- 推送前重新核实实际远程地址、目标分支和远程最新状态，明确本次将上传的全部
  提交及关联对象；不要仅依赖可能过期的本地远程跟踪引用。
- 审核完整待推送范围中的每个提交、文件版本和提交信息，检查隐私、凭据、敏感
  信息及不必要文件；包括后来已经删除但仍存在于待推送历史中的内容，不能只看
  最新差异或最终文件树。新分支或新远程还需审核随之上传的祖先历史。
- 发现问题时先处理并重新审核，未解决前不推送；推送时明确指定远程和目标分支，
  不顺带上传无关分支或标签。

## 5. 已跟踪文件与历史问题

- `.gitignore` 不会自动移除已跟踪文件或历史内容。需要停止跟踪时，审核影响后
  仅对明确路径使用 `git rm --cached -- <path>`，保留本地所需文件。
- 当前提交删除了敏感文件，不代表历史已清理；仍须检查完整待推送范围。
- 涉及重写已有历史、修改已有提交或强制推送时，先说明受影响的分支、提交、
  协作影响和恢复方式，并获得用户确认，不擅自执行。

## 6. README 与 GitHub 仓库简介

- 每次修改都检查 README 是否仍与实际情况一致。涉及功能、安装、启动、配置或
  使用方式变化时，在提交和推送前更新对应说明，并与相关代码一起提交、推送，
  不等推送完成后再补写。
- 推送成功后，检查对应 GitHub 仓库的 About 简介是否准确；如果已不准确，则
  同步更新。无关的小改动无需反复修改简介，也不把上游仓库当成本项目更新。
- 文档和简介只描述实际实现的行为，如实注明必要的验证限制，不宣称尚未实现或
  未经验证的功能；无法更新简介时如实报告。

## 7. 完成报告

- 简要说明排除规则及保留内容、实际检查结果、提交哈希和说明、是否推送及目标
  远程和分支，以及 README 和 GitHub About 是否更新。
- 如实说明未完成、失败或未验证的事项，以及尚待用户确认的历史操作；不得将
  局部检查、旧远程引用或本地提交描述为全历史审核通过、远程最新状态或推送成功。

## 8. Anki 与 SynapsePro 合并后的边界

- 用户已要求将两组功能直接内置到同一软件。活动源码是 `qt/aqt/builtin_features/synapsepro/` 与 `fsrs_helper/`，由 AnkiQt 初始化；不再生成或安装独立插件。
- 必须保留 SynapsePro 1.6.0-local 的 DeepSeek、三栏浏览以及已经完成的功能移除。FSRS Helper 使用接手的 26.05.08，不自行更换上游版本。
- 两组功能必须在同一份 Anki、同一配置内共同验证，使用合成数据；测试配置、完整安装包和用户备份不入 Git。参考 `BUILTIN-FEATURES.md`。
- 用户已授权本次正式安装及配置迁移。先备份程序、源码和用户数据，验证通过后停用旧插件。迁移保留源数据，旧插件加载过滤必须持续避免重复执行。
- 用户进一步要求：融合成功后移除旧独立插件，完整原件仅保留在恢复备份。以仓库内源码集成为工作重点，不反复扫描系统软件。
- 保留所有历史、原始相邻插件仓库及子模块。统一维护、提交和审核后推送 `main`。

## 9. 固定交付入口

- 日常启动入口固定为 `dist/启动 Anki.lnk`，实际使用目录固定为 `dist/Anki-weak-review-tables-26.8.1/`。后续功能升级继续更新此目录；不要让用户根据新功能名称或文件夹日期选择程序。
- 新建的功能构建目录只作验收或恢复副本，不替代固定入口。除非用户明确要求迁移，不重命名或移动当前使用目录。
- 验收后同步程序文件，再运行 `just anki-entry <已交付代码提交> '<本次功能说明>' '<实际交付时间>'`；无参数时保留已有版本信息并刷新入口。此命令核对交付清单并更新固定快捷方式、`dist/当前使用版本.txt` 和 `dist/CURRENT-APP.json`；不能只更新版本说明而没有更新程序。
- 最终交付直接链接固定入口，说明更新位置、已交付修订和是否需要重启。快捷方式、版本标记与程序包保持本地，不纳入 Git。
